# Server Configuration
PORT=5001

# Database Configuration
# Local: mongodb://localhost:27017/productr
# Production (Render): Use MongoDB Atlas connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/productr?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/productr

# JWT Secret - CRITICAL: Generate a strong random secret for production
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=Test_Key

# Environment
# development | production
NODE_ENV=development

# Frontend URL - CORS Configuration
# For multiple origins, separate with commas (no spaces)
# Local: http://localhost:5173,http://localhost:5174
# Production (Vercel): https://your-app.vercel.app
# Multiple: https://your-app.vercel.app,https://custom-domain.com
CLIENT_URL=http://localhost:5173
